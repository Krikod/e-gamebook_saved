{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="center">Creation of Child Relations from Chapter {{ chapter.number }}</h1>
<div class="container">
    <div class="row">
        <div class="card-panel">
    {{ form_start(form) }}



    {#<div class="row card-panel">#}
        {#<div class="input-field col s6">#}
            {#<select name="view" class="browser-default" autofocus>#}
                {#{% for chapters in toChapters %}#}
                    {#<option name="chapter[]" value="number">{{ chapters.number }}</option>#}
                {#{% endfor %}#}
            {#</select>#}
        {#</div>#}

    {#</div>#}
    {#<input type="submit">#}
    {{ form_widget(form) }}
    {#<button class="btn waves-effect waves-light" type="submit" name="action">Create relation#}
        {#<i class="material-icons right">send</i>#}
    {#</button>#}

    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('book_show', {id:book.id}) }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('chapters_show', { id: chapter.id }) }}">show chapter</a>
        </li>
        <li>
            <a href="{{ path('chapters_edit', { id: chapter.id }) }}">edit chapter</a>
        </li>
    </ul>
</div>
</div>
</div>
{% endblock %}


{% block javascripts %}

    {{ parent() }}
    <script>
        $(document).ready(function() {
            $('select').material_select();
        });
    </script>

{% endblock %}